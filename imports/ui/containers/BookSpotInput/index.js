import React from 'react';

const styles = {
  welcome: {
    height: '85vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
};

const BookSpotInput = () => (
  <div style={styles.welcome}>
    <h1>Bookspot Input</h1>
  </div>
);

export default BookSpotInput;
