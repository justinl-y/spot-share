import React from 'react';

const styles = {
  'not-found': {
    height: '85vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
};

const Booking = () => (
  <div style={styles['not-found']}>
    <h1> Booking </h1>
  </div>
);

export default Booking;
